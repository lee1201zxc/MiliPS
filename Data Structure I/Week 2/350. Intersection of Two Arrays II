### 강민수 풀이

class Solution {
public:
    vector<int> intersect(vector<int>& nums1, vector<int>& nums2) {
        vector<int> r(1001);
        for (auto x: nums1){
            r[x] += 1;
        }

        vector<int> a;
        for(int i=0;i<nums2.size();i++){
            if(r[nums2[i]]-->0){
                a.push_back(nums2[i]);
            }
        }
        return a;
    }
};

Tp: O(n) / Sp: O(n)
