## 강
class Solution {
public:
    string reverseWords(string s) {
        string answer, ss;
        stringstream st(s);
        while(st >> ss) {
            reverse(ss.begin(),ss.end());
            answer += ss + " ";
        }
        answer.resize(answer.size()-1);
        return answer;
    }
};
